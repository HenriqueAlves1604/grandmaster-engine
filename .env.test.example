# Database Configuration for Integration Tests
# IMPORTANT: Use a dedicated database to avoid data loss in development
DATABASE_URL="mysql://<user>:<password>@<host>:<port>/grandmaster_db_test"

# JWT Configuration
JWT_SECRET="test_secret_only_for_integration_tests"
JWT_EXPIRES_IN="15m"

# App Configuration
PORT=3334
NODE_ENV=test